import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { FechaPage } from './fecha';

@NgModule({
  declarations: [
    FechaPage,
  ],
  imports: [
    IonicPageModule.forChild(FechaPage),
  ],
})
export class FechaPageModule {}
